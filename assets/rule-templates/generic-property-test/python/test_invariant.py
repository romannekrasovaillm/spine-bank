"""Заготовка теста свойства: пока свойство не сформулировано, тест пропускается.

Правило `generic_property_test` в `CONSTRAINTS.yaml` ссылается на этот файл как
на команду проверки. Пока здесь стоит заглушка, тест обязан ПРОПУСКАТЬСЯ
(skip, exit code 0), а не падать: шаблон не должен краснить репозиторий,
в котором свойство ещё не сформулировано.

Как заменить заглушку:

1. Опишите свойство одной фразой в docstring теста — как спецификацию.
2. Замените `FakeEffects` (или добавьте свой фейк) в `fakes.py` так, чтобы
   он считал ровно то, что свойство запрещает или требует.
3. Уберите `pytest.skip(...)` и напишите проверки с русскими
   assert-сообщениями, называющими нарушенное свойство.
4. Продублируйте засеянное нарушение отдельной реализацией и убедитесь, что
   тест на нём падает: правило, которое никогда не краснеет, ничего не проверяет.
"""

import pathlib
import sys

import pytest

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))

# Фейк импортируется сразу: так видно, что файл лежит рядом и доступен тесту
# после переноса скелета в ваш репозиторий.
from fakes import FakeEffects  # noqa: E402,F401


def test_invariant_is_not_formulated_yet():
    """Свойство ещё не сформулировано — тест-заглушка шаблона."""
    pytest.skip("сформулируйте свойство инварианта и замените этот тест")
