{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "edit_file|write_file",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[patterns-resilience] Проверьте новые внешние вызовы: таймауты, ретраи, circuit breaker (arch-be skills search resilience)'"
          }
        ]
      }
    ]
  }
}
