---
name: spine-architect-review
description: Архитектурный разбор проекта через Spine из харнесса — маршрут значимости, модель системы, трассировка REQ→NFR→AD→CMP→правила, инвентаризация. Используй этот навык, когда архитектор просит «разбери проект», «оцени маршрут/критичность», «покажи модель/трассировку», «что сломается, если…», при старте работы в новом репозитории со Spine.
---

# Архитектурный разбор через Spine (всё read-only)

Стандартная связка (все вызовы MCP, LLM у Spine нет — выводы формулируешь ты):

1. **Маршрут**: `significance_score` с `triggers` (например
   `{"new_component": true}`, `{"security_boundary": true}`,
   `{"irreversible_migration": true}`) → route Fast/Standard/Critical.
   Всегда помечай, что маршрут — функция ЗАЯВЛЕННЫХ триггеров, а не
   истина о решении: если что-то не спросили — скажи это.
2. **Модель**: `model_query {"dir": "model"}` (список) и
   `model_query {"dir": "model", "id": "CMP-001"}` (карточка). Следи за
   сиротами (сущности без связей) — это находки.
3. **Целостность модели**: `model_validate {"dir": "model"}` → JSON-вердикт
   (битая ссылка/дубль ID/цикл depends_on — error).
4. **Трассировка**: `trace_check {"case": "."}` → цепочка
   REQ → NFR → AD/ADR → CMP → правило CONSTRAINTS. AD без правила и без
   `unverifiable` — error: предложи правило или явное обоснование.
5. **Спайн**: `spine_lint {"path": "ARCHITECTURE-SPINE.md"}` — формат
   инвариантов.
6. **AGENTS.md**: `agentsmd_lint {"repo": "."}` — свежесть канала инструкций.
7. **Количественные NFR** (если у NFR-сущностей заполнены поля ADR-007):
   `nfr_check {"path": ".", "kind": "all"}` → агрегат budget/availability/
   capacity/cost с виновными hop'ами/звеньями в `issues`.
8. **Дисциплина изменений** (если репозиторий git и ведутся дельты):
   `delta_guard {"path": "."}` → правки спайна мимо `changes/*/DELTA.md`;
   для активного change с бандлом — `evidence_verify {"change_dir":
   "changes/<name>"}` (полнота по маршруту + целостность хэшей).

## Выдача архитектору

Сводка таблицей: маршрут (+оговорки), состав модели (сироты!), покрытие
трассы, вердикт гейта. Находки — с конкретным файлом/ID и предлагаемой
чинкой. Никогда не выдумывай сущности, которых не вернул `model_query`.
