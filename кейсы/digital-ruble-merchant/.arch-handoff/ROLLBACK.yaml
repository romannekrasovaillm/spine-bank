# Якорь отката — коммит, которым кейс введён в репозиторий: откат возвращает
# кейс к состоянию «как он пришёл», а не к произвольной ревизии.
baseline_commit: ca816b0
steps:
  - name: вернуть предыдущую версию сервиса
    run: "true"
  - name: переключить трафик ТСП
    run: "true"
verify: "true"
