# Фактические исходы прогонов (2026-08-16)

Стенд: Spine (`arch-be`) оркеструет; исполнители — Claude Code headless
(`claude -p --dangerously-skip-permissions`, бэкенд `deepseek-v4-pro[1m]`),
каждый в изолированном git-worktree со своим под-пакетом `.arch-handoff/`.
Спайн `ARCHITECTURE-SPINE.md` (AD-1…AD-3) — единственный канал согласования:
исполнители не видели работу друг друга.

| Прогон | Эпик (инвариант) | Исход | Тесты | Fitness | Контракт |
|---|---|---|---|---|---|
| p1-amount | `spinecalc.amount` (AD-1) | код 0 | 5 passed | PASS (6 правил) | `complete`; 1 conflict зафиксирован* |
| p2-logfmt | `spinecalc.logfmt` (AD-2) | код 0 | 6 passed | PASS | `complete`, conflicts [] |
| p3-report | `spinecalc.report` (AD-3) | код 0 | 4 passed | PASS | `complete`, conflicts [] |
| p4-mcp | чтение пакета через MCP | пакет получен по `read_packet("p4-mcp")` | — | — | прогон прерван по idle-таймауту** |

\* p1 эскалировал через `conflicts_with_prior_decisions` (не «решил на месте»):
AD-1 декларирует аннотированную сигнатуру `validate_amount(value: float,
limit: float) -> bool`, а приёмочный regex в CONSTRAINTS.yaml требовал
`def validate_amount(value, limit)` — исполнитель реализовал без инлайновых
аннотаций (семантика bool сохранена) и явно поднял расхождение архитектору.
Это штатная работа поля conflicts: расхождение зафиксировано, а не скрыто.
Три исполнителя независимо сошлись на сигнатуре без аннотаций — согласование
прошло через контракт (TASK.md + CONSTRAINTS), а не через взаимную видимость.

\** Прогон чтения через MCP прерван тишиной 600 с (медленный прокси между
вызовами) — дефект обвязки, устранён в тот же день: таймаут тишины стал
heartbeat'ом по mtime репозитория, потолок прогона масштабируется маршрутом.

## Интеграционный гейт

Склейка трёх модулей в единый пакет `spinecalc` (см. `spinecalc/__init__.py`)
прошла с первой сборки: сквозной сценарий «валидация → лог → отчёт» —
`INTEGRATION OK` (см. `integration-run.txt`), `spine_lint` по спайну —
«нарушений нет».

## Что кейс вскрыл (и что было исправлено в харнессе в тот же день)

1. Исполнители завершились `complete`, но **без финального git-коммита** —
   контракт его не требовал → в TASK.md добавлена секция «Финализация»
   (коммит обязателен) + авто-коммит хвоста харнессом (`auto_commit`).
2. Первый прогон Critical-кейса в другом эксперименте оборвался по 30-мин
   потолку → таймаут масштабируется маршрутом значимости (Fast/Standard/
   Critical → 1800/3600/7200 с, MANIFEST.json).
3. JSON-контракт извлекался эвристикой → механический разбор со схемой
   (`Valid`/`Invalid`/`Missing`), эскалация blocked/conflicts, коды выхода
   CLI 2/3 для скриптовых гейтов.
