# Кейсы — учебные примеры цикла solution-архитектора

> Training cases produced with the Spine domain harness: end-to-end
> solution-architecture work products, from spine invariants to a code-harness
> handoff package. Synthetic and educational — not production designs.

Эта папка — коллекция учебных кейсов, подготовленных доменным харнессом
Spine (`arch-be`). Каждый кейс — сквозной, самодостаточный пример работы
solution-архитектора с харнессом: от архитектурных инвариантов (spine) до
пакета передачи кодовому харнессу. Кейсы читают люди и агенты: как эталон
структуры артефактов, как материал для рубрик и как вход для обучения.

## Реестр кейсов

| # | Кейс | Домен | Модель | Что показывает |
|---|------|-------|--------|----------------|
| 001 | [sbp-gateway](sbp-gateway/) | Банковские платежи: СБП, C2B-эквайринг | DeepSeek V4 Flash | Полный цикл: spine → solutioning → ADR → контракты/NFR → handoff кодовому харнессу |
| 002 | [payment-processing-platform](payment-processing-platform/) | Процессинг банка: рельсы (карты/СБП/SWIFT/БЭСП), DDD | GLM-5.2 | Greenfield Critical за одну сессию: 27 инвариантов spine, 16 ADR, fitness-констрейнты под код, walking skeleton |
| 003 | [govproc-platform](govproc-platform/) | Электронная коммерция госсектора: B2G-закупки (44-ФЗ/223-ФЗ), ЕИС, УКЭП | Kimi K3 | Компактный комплект: 7 AD spine, 5 ADR, NFR, OpenAPI-контракт, эмулятор внешней системы в walking skeleton |
| 004 | [parallel-epics](parallel-epics/) | Параллельная разработка библиотеки: 3 эпика × 3 исполнителя | Claude Code ×3 (бэкенд deepseek-v4-pro) | Параллельный флот по worktree: spine склеил стыки без взаимной видимости исполнителей (15/15 тестов, интеграция с первой сборки), handoff-пакет по MCP, дефекты → фиксы харнесса |
| 005 | [fleet-of-ten](fleet-of-ten/) | Массовый параллелизм: 10 эпиков × 10 исполнителей | Claude Code ×10 (бэкенд deepseek-v4-pro) | Десять параллельных эпиков за ~3,2 мин стены: 10/10 complete, 42/42 тестов, 60/60 fitness, интеграция с первой сборки; флот САМ закоммитил работу (контракт «Финализация») |
| 006 | [drift-control](drift-control/) | Дрейф-эксперимент: платёжное ядро на Rust, две руки | Claude Code (бэкенд deepseek-v4-pro) | Контролируемый A/B-прогон: голая задача → гейт FAIL 2/6 exit 1 (нет thiserror, нет идемпотентности — дрейф при зелёных тестах), задача + handoff-пакет → PASS 6/6 с настоящим inbox идемпотентности; воспроизводимо из репозитория |
| 007 | [fleet-spine-drift](fleet-spine-drift/) | Флот worktree с полными копиями спайна (платформа инференса: gateway/router/registry) | — (механический кейс, без LLM) | Модель «5.2 + дельта-протокол» в миниатюре: `arch-be fleet audit` считает дубли 66.7% и дрейф `CONSTRAINTS.yaml` (отступник wt-c, exit 1), `arch-be delta guard` запрещает прямые правки спайна мимо `changes/<id>`; воспроизводится голым бинарём |
| 008 | [legacy-survey](legacy-survey/) | Brownfield legacy: монолит без документации (python+sql+shell+csv) | — (механический кейс, без LLM) | Reverse discovery как функция: `arch-be survey` собирает карту обследования по 9 артефактам (скрытая связь — общая таблица payments у двух «сервисов», drop-каталог csv, мёртвый флаг), confirmed со ссылками `файл:строка` и честные `[gap]`; свежесть карты — гейт `max_age` в CONSTRAINTS.yaml |
| 009 | [jvm-archunit-gate](jvm-archunit-gate/) | JVM-гейты: многослойное java-приложение (синтетика, 3 слоя) | — (механический кейс, без LLM) | ArchUnit-мост (ADR-039): структурные правила из одного CONSTRAINTS.yaml исполняются настоящим ArchUnit — standalone-гейт ловит нарушение слоёв по байткоду (FAIL exit 1), зеленеет после фикса; тот же YAML гоняется нативно и рендерится во встраиваемый JUnit-тест |
| 010 | [digital-ruble](digital-ruble/) | Цифровой рубль для физлиц: банк-участник платформы Банка России | Claude Code (без LLM у Spine) | Полный архитектурный комплект маршрута Critical в режиме «Spine Core без своей LLM»: 65 сущностей модели, 10 инвариантов, 15 fitness-правил, 9 ADR, 5 контрактов, NFR-математика — и evidence FAIL с блокировкой выпуска (гейт сказал «нет») |

## Кейс 001 — `sbp-gateway` (платёжный шлюз СБП, C2B-приём)

Проектирование платёжного шлюза банка к Системе быстрых платежей: приём
C2B-платежей от ТСП через ОПКЦ СБП (АО «НСПК»), зачисление в АБС, возвраты.
Маршрут **Critical** (значимость 11/15). Подготовлен на модели **DeepSeek V4
Flash** (`deepseek` в `MANIFEST.json` — дефолтный flash-эндпоинт харнесса).

Чему учит кейс:

- **Spine-инварианты как связующий каркас**: `ARCHITECTURE-SPINE.md` —
  восемь решений AD-001…AD-008 с Binds/Prevents/Rule и fitness-проверками;
  статусы Proposed/Adopted, секция Deferred с условиями возврата.
- **Человеческие гейты**: стратегия реализации (собрать ядро / купить
  транспорт / гибрид) вынесена в ADR-007 и закрыта решением A3 человеком —
  в spine отмечено `[ADOPTED]` с датой.
- **Честная работа с внешними входами**: протокол НСПК публично не раскрыт —
  все протокольные детали помечены `[ТРЕБУЕТ ПРОВЕРКИ]`, контракт
  `nspk-contract.md` явно отложен «после получения документации».
- **Handoff-пакет кодовому харнессу** (`.arch-handoff/`): задача walking
  skeleton с headless JSON-контрактом результата
  (`complete|partial|blocked` + assumptions/open_questions/conflicts),
  дистиллированный epic-context (1500 токенов, см. `MANIFEST.json`),
  инварианты (`ARCHITECTURE.md`), fitness-правила (`CONSTRAINTS.yaml` —
  исполняются `arch-be control check`), якорная рубрика приёмки
  (`RUBRIC.yaml`, шкала 1–5 с текстовыми якорями).
- **Контрактная независимость ядра**: внутренний контракт адаптера ОПКЦ —
  единственная зависимость ядра; RFP-пакет вендору транспорта —
  `docs/rfp/vendor-rfp.md`.

Анатомия кейса:

```
ARCHITECTURE-SPINE.md     инварианты AD-001…AD-008 (Binds/Prevents/Rule)
README.md                 обзор, статус, карта документов
docs/
  solutioning.md          полный дизайн: C4, потоки, гейты A0–A5, gaps, откат
  nfr.md                  измеримые NFR (доступность 99,95%, лаги, RPO=0)
  contracts/tsp-api.md    мерчант-API v0.1 (Idempotency-Key, вебхуки)
  contracts/opkc-adapter.md  внутренний контракт адаптера ОПКЦ (ядро ↔ транспорт)
  spec/state-machine.md   статусная машина платежа: переходы, инварианты
  rfp/vendor-rfp.md       RFP вендору транспорта (критерии, POC, SLA)
  adr/ADR-001…007.md      решения: outbox, статусная машина, mTLS, DLQ,
                          идемпотентность/зачисление из PAID, trust-зоны, стратегия
.arch-handoff/            пакет передачи кодовому харнессу
  TASK.md                 задача + контракт результата (headless JSON)
  ARCHITECTURE.md         epic-context: дистиллят ADR/solutioning для исполнителя
  CONSTRAINTS.yaml        fitness-правила (file_exists / must_contain / must_not_contain)
  RUBRIC.yaml             якорная рубрика handoff_quality (5 критериев, веса, якоря 1/3/5)
  MANIFEST.json           модель, источники, бюджет epic-context
  adr/                    копии ADR, замороженные на момент передачи
```

## Кейс 002 — `payment-processing-platform` (процессинг платежей, Greenfield)

Целевая архитектура центрального процессинга банка: платёжные рельсы
(карты MIR/Visa/MC, СБП, SWIFT, БЭСП, внутренние переводы), декомпозиция по
bounded context (DDD), маршрут **Critical** (Significance Score 13).
Спроектирован на модели **GLM-5.2** за одну сессию по единственному вводу
«спроектируй промышленную платформу процессинга». (Handoff-пакет генерировался
позже отдельным запуском — его `MANIFEST.json` фиксирует модель на момент
генерации: `deepseek`.)

Чему учит кейс:

- **Другая форма результата на другой модели**: в отличие от кейса 001
  (solutioning + NFR + контракты + RFP), GLM-5.2 выдал единый целевой
  `ARCHITECTURE.md` (458 строк: рельсы с latency-бюджетами и регуляторами,
  bounded contexts, NFR, риски) + 27 инвариантов spine + 16 ADR. Полезно для
  сравнения «модель ↔ стиль артефактов».
- **Spine как инструктаж параллельных исполнителей**: 27 инвариантов
  AD-1…AD-27 со статусами `[ADOPTED]` — от сага-оркестрации и outbox до
  жизненного цикла данных (152-ФЗ vs append-only аудит) и chaos engineering.
- **Fitness-констрейнты уже под код**: `CONSTRAINTS.yaml` — 24 правила с
  severity `critical` и glob'ами по Go-файлам/миграциям (`Idempotency-Key`
  в каждом mutating endpoint, outbox-таблица в миграциях) — готовы к проверке
  реализации `arch-be control check`, а не только «до кода» как в кейсе 001.
- **Исполнимый handoff**: walking skeleton H0 — внутренний перевод A→B сквозь
  9 шагов (gateway → сага → холд → комиссия → двойная проводка → outbox →
  идемпотентный consumer). Критерии приёмки проверяемы командами
  (`go build`, `go test`, SQL до/после), негативные сценарии перечислены
  (повторный запрос, poison message → DLQ, 409 на недопустимый переход),
  план отката — удаление namespace.
- **Naming-линт слагов — в коде, а не руками**: ранее кириллические
  заголовки ADR деградировали в имена вроде `ADR-004-4.md` — после
  транслитерации в `kebab_slug` (ADR-002 в `docs/adr/` харнесса) имена
  детерминированы и навигационны
  (`ADR-004-segmentaciya-doverennyh-zon-4-zony.md`).

Анатомия кейса — в его [README.md](payment-processing-platform/README.md).

## Кейс 003 — `govproc-platform` (электронная коммерция госсектора, B2G)

Платформа электронной коммерции для госсектора: B2G-закупки по 44-ФЗ/223-ФЗ —
извещение заказчика, подпись УКЭП, публикация в ЕИС, заявка поставщика,
протокол. Спроектирована на модели **Kimi K3** (сессия «создай платформу
электронной коммерции с нуля для госсектора»). (Handoff-пакет генерировался
позже отдельным запуском — его `MANIFEST.json` фиксирует `deepseek`.)

Чему учит кейс:

- **Третий стиль артефактов — третья модель**: Kimi K3 выдал самый компактный
  комплект — 7 инвариантов spine, 5 ADR, NFR с профилем «дедлайн», C4 как
  mermaid-исходник и **OpenAPI-контракт как первоклассный артефакт**
  (`api/openapi.yaml`, контрактные тесты на гейте A4). Три кейса = три
  модели = три узнаваемых почерка — материал для сравнения.
- **Модульный монолит — осознанно**: ADR-005 (и on-prem, ADR-002) выбирают
  простой deployment-юнит для госсектора — контраст с микросервисным кейсом
  002; выбор обоснован через ограничения контура, а не моду.
- **Скептический дизайн вокруг внешней системы**: ЕИС — только через outbox +
  адаптер с ретраями/circuit breaker и дедупликацией; на skeleton — эмулятор
  со сбоями, не реальный zakupki.gov.ru; тест устойчивости: при мёртвом ЕИС
  приём заявок работает, публикация доезжает после «восстановления» без дублей.
- **Юридическая неизменяемость как код**: аудит append-only с hash-chain и
  запретом UPDATE/DELETE на уровне БД-роли (AD-4); криптография строго в
  модуле signing (AD-7) с честным маркером `FIXME-GOST` — SHA-256 на стенде
  вместо сертифицированного СКЗИ, с ссылкой на ADR-004.
- **Честные gaps**: профиль нагрузки в NFR помечен `[ТРЕБУЕТ ПРОВЕРКИ]` —
  гипотеза до обследования заказчика, а не выдуманные цифры.

Анатомия кейса — в его [README.md](govproc-platform/README.md).

## Кейс 004 — `parallel-epics` (параллельный флот исполнителей, спайн как клей)

Три независимых **Claude Code headless** (бэкенд `deepseek-v4-pro[1m]`)
параллельно реализуют три эпика одной библиотеки `spinecalc` — каждый в
изолированном git-worktree со своим под-пакетом `.arch-handoff/`, не видя
работу друг друга. Единственный канал согласования — спайн
`ARCHITECTURE-SPINE.md` (AD-1…AD-3: Binds/Prevents/Rule). Итог: стыки
сошлись с первой сборки, 15/15 тестов зелёные, сквозной интеграционный
тест «валидация → лог → отчёт» — OK. Отдельный канал передачи: пакет отдан
четвёртому агенту через MCP-сервер `arch-handoff` (`read_packet` по id).

Чему учит кейс:

- **Spine — контракт для параллелизма**: расхождение сигнатур предотвращено
  дословными Rule + приёмочными regex в CONSTRAINTS.yaml, а не ревью
  после слияния.
- **Поле conflicts — рабочая эскалация**: p1 зафиксировал расхождение
  «аннотированная сигнатура в AD-1 против regex приёмки» через
  `conflicts_with_prior_decisions` вместо тихого отклонения.
- **Дефекты — часть кейса**: прогон вскрыл четыре дефекта обвязки (нет
  финального коммита, короткий таймаут, эвристика разбора контракта,
  idle-таймаут на медленном прокси) — все исправлены в харнессе в тот же
  день; перечень и след «дефект → фикс» — в `evidence/runs.md`.
- **Цветные кадры прогона** — `screenshots/` (генерируются из
  `src/tui/shot.rs`, тест `gen_case04_screenshots`).

Анатомия кейса — в его [README.md](parallel-epics/README.md).

## Кейс 005 — `fleet-of-ten` (десять параллельных эпиков, флот коммитит сам)

Масштабирование кейса 004 вдесятеро: **десять Claude Code headless**
(бэкенд `deepseek-v4-pro[1m]`) параллельно реализуют десять эпиков
библиотеки `bankcalc` (валидация, округление, комиссии, курсы,
идентификаторы, календарь, ретраи, НДС, лимиты, сводка) — каждый в
изолированном git-worktree, канал согласования один: спайн AD-1…AD-10.
Оркестрация — CLI Spine (`arch-be handoff` / `arch-be harness-run`).

Фактура прогона (2026-08-16, `evidence/runs.md`):

- **Стена ~3,2 мин на десять эпиков**: прогоны 103–191 с, все код 0;
  **10/10 `complete`**, 42/42 тестов, 60/60 fitness-правил, `spine_lint`
  чист, сквозной сценарий «день процессинга» — INTEGRATION OK с первой
  сборки.
- **Контракт «Финализация» сработал 10/10**: каждый исполнитель сам
  закоммитил результат поверх baseline (`evidence/commits.txt`), авто-коммит
  харнесса не понадобился ни разу — прямое сравнение с утренним флотом
  кейса 004, не закоммитившим ничего.
- **Ноль конфликтов**: аннотированные сигнатуры спайна и regex приёмки
  согласованы заранее — урок из конфликта p1 кейса 004 применён.
- **Цветные кадры** — `screenshots/` (тест `gen_case05_screenshots`).

Анатомия кейса — в его [README.md](fleet-of-ten/README.md).

## Кейс 006 — `drift-control` (дрейф-эксперимент: две руки, один гейт)

Контролируемый ответ на вопрос «удерживает ли спайн кодовый харнесс от
архитектурного дрейфа». Одна и та же задача (Rust-библиотека платёжного
ядра) отдана реальному **Claude Code 2.1.233 headless** (бэкенд
`deepseek-v4-pro[1m]`) дважды: рука A — голый текст задачи, рука B — тот же
текст + handoff-пакет (`ARCHITECTURE-SPINE.md` из трёх инвариантов AD-1…3
+ `CONSTRAINTS.yaml` из шести fitness-правил C-01…06). Судья —
механический гейт `arch-be control check`, один для обеих рук.

Фактура прогона (2026-08-17, `evidence/runs.md`):

- **Рука A (контроль) — FAIL, exit 1**: 2 нарушения из 6 правил — нет
  thiserror (C-02) и нет идемпотентности (C-05). При этом код добротный:
  6 модулей, 32 зелёных теста, f64 для денег модель не выбрала сама —
  дрейф ровно по орг-специфичным инвариантам, невидимый без гейта.
- **Рука B (спайн) — PASS 6/6**: thiserror 2.0.20 — реальная зависимость,
  идемпотентность — настоящий inbox в `PaymentProcessor::authorize`, а не
  строка под линтер; исполнитель сам сверился с критериями приёмки.
- **Цена спайна по стене — нулевая**: 360.4 с против 371.9 с.
- **Воспроизводимо из репозитория**: обе руки лежат в кейсе
  (`armA-solution/`, `armB-solution/`), гейт перепроверяется одной командой
  (`arch-be control check … --constraints handoff-example/CONSTRAINTS.yaml`).
- **Цветные кадры** — `screenshots/` (тест `gen_case06_screenshots`).

Чему учит кейс:

- **Дрейф невидим без механического гейта**: зелёные тесты и чистая
  история коммитов не означают соблюдение орг-стандартов.
- **Модель не угадывает орг-инварианты**: они должны доезжать до
  исполнителя явно, дословно записанными в пакете, — это и есть работа
  solution-архитектора.
- **Правила должны быть исполняемыми**: не проза в вики, а
  `must_contain`/`command_succeeds` с exit-кодом для CI.

Анатомия кейса — в его [README.md](drift-control/README.md).

## Кейс 007 — `fleet-spine-drift` (дрейф спайна во флоте worktree)

Учебный кейс по мотивам разбора реального флота агентной разработки (15
git-worktree × полная копия спайна, ~90% файлов-дублей, дрейф копий
87/88/89 файлов на worktree) — в миниатюре на выдуманной платформе
инференса: три «worktree» компонентов (gateway, router, registry) в
`fleet/wt-{a,b,c}/`, у каждого полная копия спайна (`ARCHITECTURE-SPINE.md`,
`CONSTRAINTS.yaml`, `model/adr/ADR-001.md`) + один уникальный файл
компонента; в wt-c `CONSTRAINTS.yaml` намеренно ослаблен (severity
error → warn) мимо дельта-протокола. Модели нет — кейс механический,
воспроизводится голым бинарём `arch-be`.

Чему учит кейс:

- **Модель «5.2 + дельта-протокол» как инструментарий**: спайн — в одной
  копии (SSOT), компонент несёт lean-дельту, изменения спайна — только
  дельтами `changes/<id>`; кейс показывает три опоры модели в Spine —
  `arch-be fleet audit` (дубли + дрейф копий как exit-код), `arch-be delta guard`
  (запрет прямых правок спайна мимо активной дельты) и SPEC.md в
  handoff-пакете (верифицируемые контракты вместо прозы компонента).
- **Дрейф — это числа и exit code**: аудит даёт 8/12 точных дублей (66.7%),
  ядро 3 файла, ровно один расходящийся файл с поимённым отступником wt-c —
  эталон зафиксирован в `expected/audit.txt` (снят реальным запуском).
- **Гейт следит за протоколом, не за намерениями**: `arch-be delta guard`
  пропускает правку `model/` только при упоминании файла в АКТИВНОЙ дельте;
  архивные дельты не засчитываются — шаги воспроизведения на временном
  репозитории (mktemp + git init) — в README кейса.

Анатомия кейса — в его [README.md](fleet-spine-drift/README.md).

## Кейс 008 — `legacy-survey` (обратное обследование legacy-монолита)

Учебный кейс функции reverse discovery: синтетический legacy-монолит без
документации (20 файлов, python + sql + shell + csv), по которому
детерминированный сканер `arch-be survey` (без LLM) собирает каркас карты
обследования по 9 артефактам скилла `reverse-discovery`. Модели нет — кейс
механический, воспроизводится голым бинарём `arch-be`.

Чему учит кейс:

- **Скрытые связи видны механически**: два «сервиса» (billing, notifier) и
  импортёр работают с одной общей таблицей `payments` — сканер перечисляет
  все стороны со ссылками `файл:строка`; файловый обмен — drop-каталог csv.
- **Честные метки**: находки — `[confirmed]` с доказательствами, секции без
  находок — `[gap]` с вопросом владельцу (у монолита это версии платформы и
  git churn); карта не дозаполняется правдоподобным текстом.
- **Свежесть как гейт (BF-3)**: `CONSTRAINTS.yaml` кейса несёт правило
  `max_age` (365 дней) на `docs/reverse/survey.md` — просроченная карта
  обследования роняет `arch-be control check`, а не тихо устаревает в вики.

Анатомия кейса — в его [README.md](legacy-survey/README.md).

## Кейс 009 — `jvm-archunit-gate` (ArchUnit-гейт из единого источника)

Учебный кейс ArchUnit-моста (ADR-039): мини Java-приложение из трёх
пакетов-слоёв (`domain`, `application`, `infrastructure`) с намеренным
нарушением — `domain` импортирует `infrastructure` и запрещённый «внешний
FX-пакет» (`com.external.fx`, синтетика). Правила описаны один раз в
`CONSTRAINTS.yaml` кейса (два `dependency_direction` с java-glob'ами +
правило типа `archunit`); модели нет — кейс механический, воспроизводится
голым бинарём `arch-be` + JDK (jar'ы ArchUnit — `arch-be archunit fetch`).

Чему учит кейс:

- **Один YAML — два исполнителя**: нативная текстовая проверка импортов и
  JVM-нативный ArchUnit-гейт (по байткоду) читают один CONSTRAINTS.yaml;
  нарушение ловится дважды, id правила (C-01/C-02) — в каждой находке.
- **Бесшовный standalone-путь**: `arch-be archunit check` сам находит
  классы, компилирует раннер в кэш и запускает `java` — в Java-репо ничего
  не встраивается; FAIL exit 1 на нарушении, PASS exit 0 после фикса
  (эталоны — `expected/`, сняты живыми прогонами).
- **Встраиваемый путь**: `arch-be archunit gen` рендерит идиоматичный
  `ArchFitnessTest.java` (JUnit 5 + ArchUnit, `@AnalyzeClasses`, `@ArchTest`
  с id правил) для репо команды + `archunit-rules.json`.
- **Fail-closed**: нет классов/jar'ов — понятная ошибка с подсказкой, а не
  молчаливый PASS.

Анатомия кейса — в его [README.md](jvm-archunit-gate/README.md).

## Кейс 010 — `digital-ruble` (цифровой рубль для физлиц, маршрут Critical)

Проектирование сервиса банка-участника платформы цифрового рубля Банка
России: кошелёк открывается на платформе, банк — участник. Кейс собран в
живом эксперименте «Spine Core без своей LLM» (отчёты —
`docs/experiments/`): проектировал внешний агент (**Claude Code**), все
гейты исполнял `arch-be` без единого API-ключа. Маршрут **Critical** (10
триггеров из 15). Комплект полный: 65 сущностей модели (161 связь), 10
инвариантов spine, 15 fitness-правил с карточками, 9 ADR, 5 контрактов,
NFR-математика (доступность цепочки 99,4004%, latency-бюджеты, TCO 46,86
млн ₽/год), evidence bundle.

Чему учит кейс:

- **UNKNOWN-состояние как несущий элемент**: таймаут платформы — не успех
  и не отказ, а обязательное состояние машины операции (AD-005) с выходом
  только по подтверждению платформы; повтор из неопределённого состояния
  запрещён.
- **Проекция платформы ЦР, а не второй баланс**: источник истины —
  платформа (AD-001); в банке проекция с меткой свежести и признаком
  `derived`; **сверка — постоянный контур с измеримым лагом** 5 мин
  (AD-007, NFR-009) без автоматической правки расхождений.
- **Append-only журнал** — единственный источник аудита и аргументов в
  спорах (AD-002); протокол ПЦР не раскрыт — детали честно помечены
  `[ТРЕБУЕТ ПРОВЕРКИ]`, как в кейсе 001.
- **Маршрут Critical — гейт, а не самооценка**: score форсирован
  неотключаемыми триггерами; человеческий гейт A3 (стратегия реализации,
  ADR-009) остался Proposed — и закрыть его за человека нельзя.
- **Блокировка выпуска — работа гейта, а не сбой**: `evidence verify` —
  FAIL, exit 1 (4 артефактов стадии реализации отсутствуют, спайн изменён
  после упаковки бандла — пойман по хэшу). Единственный кейс коллекции,
  где evidence-гейт сказал «нет»: пакет не выпускаем, пока нет
  доказательств следующей стадии.
- **Пять ценностей Spine измерены** на этом кейсе (проверяющий «не я»,
  схема полноты, проверяемые числа, governance, передача) — сводка в
  README кейса, полная оценка с честными границами — в отчётах
  `docs/experiments/`.

Анатомия кейса — в его [README.md](digital-ruble/README.md).

## Конвенции для новых кейсов

- **Имя папки**: `NNN-slug` не требуется — достаточно короткого slug
  (`sbp-gateway`); номер присваивается строкой в реестре выше. Дубликатов
  slug быть не должно.
- **Обязательный минимум**: `README.md` кейса (обзор + статус + карта
  документов) и хотя бы один содержательный артефакт цикла. Эталон полного
  набора — `sbp-gateway`.
- **Модель фиксируется**: в реестре этой папки и (для handoff-пакетов) в
  `MANIFEST.json`. Это позволяет сравнивать кейсы между моделями.
- **Только относительные пути** внутри кейса (источники в манифестах,
  ссылки между документами) — кейс обязан быть переносимым.
- **Без секретов и персоналки**: никаких ключей, абсолютных домашних путей,
  имён сотрудников и внутренних систем реальных компаний. Кейсы синтетические.
- Кейс — учебный артефакт: он демонстрирует метод и формат, а не является
  промышленным дизайном (см. research-дисплеймер в корневом README).
