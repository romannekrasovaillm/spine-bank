# ROADMAP — Spine Banking Edition

Снимок: 2026-09-04 (статусы треков, пришедших волнами 1–3 бэклога
spine-core-improvements, обновлены 2026-09-19 — подготовка v0.3.4). Документ
пересматривается владельцем не реже раза в
квартал и по завершении каждого пункта (статус обновляется в том же
коммите). История и обоснования — в ADR (`docs/adr/`), не здесь.

## Принципы планирования

- Каждое изменение ядра — через ADR + handoff-конвейер (ADR-015);
  fitness-гейты обновляются в том же коммите (AD-9).
- Приоритет: сначала доказуемость для ревью/пилота ДИТ, потом новые
  функции. Ревью 2026-09 обработано планом
  `predlozheniya_dorabotke_spine_be_2026-09-04.md` (статусы — там).

## Горизонт 1 — пилотная готовность (Q4 2026)

| # | Трек | Статус 2026-09-04 |
|---|---|---|
| Транши инструментальной поверхности (ADR-015): openapi-lint → asyncapi → contract chain | **done 2026-09-19** (волны 1–3: T1 — верификаторы `nfr_check`/`model_validate`/`delta_guard`/`evidence_verify` мостом в MCP; T2 — реестры `landscape_report`/`adr_registry`/`rules_report`/`openspec_coverage`/`model_graph`; T3 — составные `architect_review`/`change_impact`; contract chain расширен: proto/Avro/JSON Schema/DDL + `--model`; T4 — чтение знаний) | done |
| Закрытие ревью: fitness-структуры, EA-экспорт, метрики, угрозы, процесс | 11 из 12 замечаний закрыто (P0+P1) | done |
| Brownfield-функция: `arch-be survey` + кейс 008 | **done 2026-09-19**: `arch-be survey` (CLI + MCP `reverse_survey` под `--rw`), кейс 008 `кейсы/legacy-survey/` в репо | done |
| Ландшафт систем (EA-3), ранбук архкомитета (PR-4) | **ландшафт done 2026-09-19** (`arch-be model landscape` + алиасы/дифф, MCP `landscape_report`, ADR-037); ранбук архкомитета — в работе (P2) | частично |
| Модель угроз: sandbox bwrap + injection-гигиена (T-4) | **done 2026-09-19**: ADR-038, `[bash] sandbox = "bwrap"` (`src/tools/bash.rs`), injection-гигиена — `src/injection.rs`, живой реестр — `docs/threat-model.md` | done |
| Пилот ДИТ: деплой bank-profile, первые outcome-данные (дефекты, MAE людей, распределение маршрутов) | подготовка; инструментарий outcome-данных пришёл волной 2 (2026-09-19): журнал `.arch-handoff/mcp-calls.jsonl`, `arch-be digest`, регистр FP (`control fp mark`) | — |

## Горизонт 1.6 — смысловой слой (2026-09-20, 0.3.5)

| # | Трек | Статус 2026-09-20 |
|---|---|---|
| Досье судьи, направление доказательства, покрытие (ADR-051) | done: `src/rubric_pack.rs`, поля критерия `evidence_on`/`evidence_roles`/`coverage`, CLI `rubric pack`, MCP `pack`/`subject` | done |
| Четыре смысловые рубрики (D6/D10/D11 + обещание без механизма) | done: `assets/rubrics/`, вид досье объявлен в рубрике (`pack:`) | done |
| Golden-набор смысловых рубрик | done: 4 × 6 досье, эталон-диапазон, раздельный счёт ложных обвинений и пропусков; предварительный замер — `docs/experiments/semantic-rubrics-2026-09-20.md` | done |
| Смысловая строка red-team | done: колонка в карте, `--keep-semantic`, `redteam semantic-score`; в долю 11/14 не входит | done |
| Составляющая `semantic_quality` (ADR-052) | done: выключена по умолчанию, область `changed`/`all`, находки `semantic_*` | done |
| **Дальше:** второй независимый судья для цифр приёмки | открыто: нужен харнесс/модель, отличная от автора рубрик, иначе `judge_is_author` | **[РЕШЕНИЕ ЧЕЛОВЕКА]** |
| **Дальше:** тюнинг рубрик по замерам | открыто: первый замер показал пропуски засеянных дефектов и ложное обвинение на «трудном чистом» — якоря и промпт требуют калибровки на живом судье | открыто |
| **Дальше:** типизация связей модели как механическая проверка D6 | открыто: рубрика ловит смысл, но не запрещает неверный конец ссылки конструктивно | открыто |

## Горизонт 2 — волны библиотеки правил (Q4 2026 – Q1 2027)

- Волна 2 (обязательное извне, ADR-019): threat model — **done**
  (docs/threat-model.md, ADR-031); ПДн-регионы — следующая карточка.
- Волна 3 (смысловое): границы доменов/данных — через скиллы ru-*,
  не CONSTRAINTS (позиция ADR-019).
- Волна 3 (исполняемое): библиотека шаблонов исполняемых правил, детектор на
  каждый инвариант, проверка зубов — **done 2026-09-20** (ADR-050, девять
  шаблонов на python/Java, `arch-be rules template …`). Следующая карточка:
  шаблоны структурных типов (`dependency_direction`, `context_boundary`,
  `archunit`) — вне объёма этой волны; рубрика `rule_invariant_fit`
  (соответствие теста смыслу инварианта) — в задании про смысловые рубрики.
- Калибровка значимости по данным пилота: пороги `[significance]` —
  рабочая гипотеза (ADR-034), пересмотр по `significance-cases.yaml`.

## Горизонт 1.5 — что перенесено из 0.3.4 (2026-09-19)

Волны A, B, C и W закрыты; ниже — то, что осталось **незакрытым**, с причиной
переноса. Подробности — `docs/experiments/0.3.4-что-сделано.md`.

| # | Пункт | Почему не закрыт |
|---|---|---|
| 1 | Версия `Cargo.toml → 0.3.4`, `CHANGELOG`, релизные ассеты и `SHA256SUMS` | DoD раздела 8 ТЗ закрыт не весь: поднятая версия выдаёт неполное за релиз |
| 2 | `adr_quality` по ADR-041/042/043 судьёй, **отличным от автора** | В окружении нет второй модели: судья — та же модель, что писала документ. ADR помечены строкой `Judge: judge_is_author`; механически помеченный отчёт есть у ADR-001 кейса `salary-payments` (split-judge канал прогнан целиком) |
| 3 | Сравнение с релизным ассетом 0.3.3 из GitHub Releases | Стенд собирал 0.3.3 из того же коммита `9486d79` локально; сравнение с ассетом из Releases не делалось |
| 4 | Живой прогон пяти сценариев во внешних кодовых харнессах (Claude Code, Qwen Code, omp) | Каналы Spine (CLI, MCP, плейбуки, скиллы, хуки, headless) пройдены на новом кейсе, но не живой сессией чужого харнесса — как и в исходном исследовании |
| 5 | Хуки, установленные в чужой харнесс | Сценарий «ослабление правила после коммита» прогнан той же командой, что вызывает pre-push (`gate --base HEAD~1...HEAD`); интеграционный тест с bare-remote есть (Н5) |
| 6 | Мутационное измерение кейса `salary-payments` ≥ порога | 10 из 14 = 71 %: D9 требует включённой `decision_quality` (см. п.2), D11 механике недоступен по построению. Порог не понижался, чтобы «пройти» |
| 7 | Скиллы `spine-*` под новые инструменты (`bootstrap`, `trust`, `redteam`) | Плейбук `spine-bundle` создан; отдельные скиллы под `trust`/`redteam` — следующий шаг |

## Горизонт 3 — платформа (2027)

- Ландшафт и реестры как агрегаты (ADR-036/037) — развитие по запросам
  пилотов: watch-режим, дифф ландшафта между датами.
- Цепочка поставки: подпись артефактов (cosign), SLSA-provenance
  (docs/supply-chain.md, честный реестр).
- Импорт ArchiMate (обратный мост EA) — при подтверждённом сценарии
  заказчика (ADR-032 фиксирует export-only как сознательное ограничение).

## Чего в планах нет (осознанно)

- Серверный/многопользовательский режим, RBAC — ADR-033 (single-user
  by design; процессная обвязка — снаружи, файловая поверхность).
- YandexGPT в модельной матрице — ADR-012 (продуктовое ограничение).
- GigaChat-прогоны evidence — решение владельца 2026-09-04 (фокус на
  open-source внутри периметра; адаптер GigaChat поддерживается, ADR-021).
