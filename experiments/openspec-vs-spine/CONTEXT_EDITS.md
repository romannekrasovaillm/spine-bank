# CONTEXT_EDITS.md — журнал ручных вмешательств в контексты

Формат-контроль после генерации контекстов (2026-09-05T22:55):

- **T02_C.md — перегенерирован** (не ручная правка текста, а повторный вызов
  тем же нейтральным промптом к той же модели glm-5.3). Причина: поломка
  формата — модель вместо спайн-инвариантов выдала Rust-код (```rust …```).
  Перегенерация с добавлением к промпту «и без кода» → формат корректный
  (7 инвариантов «ЗАПРЕЩЕНО … Проверка: …», 1634 знака). Событие записано
  в contexts/_gen_log.jsonl (regen: true).
- Остальные 19 контекстов: формат корректный без вмешательств
  (C: все начинаются с «1. ЗАПРЕЩЕНО …»; B: 8–12 SHALL-требований +
  «# Design» + «# Rules» у всех 10).

Больше правок не было. Тексты контекстов не редактировались вручную.
